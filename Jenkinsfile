pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }
    stage('Release') {
      steps {
        echo 'Ready to release etc.'
      }
    }
  }
}
